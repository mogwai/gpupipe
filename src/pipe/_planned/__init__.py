"""Preserved-but-inactive features.

Modules here are intentionally NOT imported by the `pipe` package. They hold
complete implementations of features that have been pulled from the live code
path but kept for a future re-integration. See `PLANNED.md` at the repo root
for the design rationale and the exact wiring points each module needs.
"""
